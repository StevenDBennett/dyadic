# Unified Research Guidance: Dual-View Arithmetic × Butterfly Compiler

> This is a dyadic (3-package monorepo) document, ported from the dual-view project.

**Synthesis Document** — Pulling together the 2-adic Newton dynamics, the Universal Butterfly Compiler, and the Quantum Dual-View framework into a single research program.

---

## 1. The Core Insight

Your two independent threads are actually the same object viewed from opposite ends:

| Thread | Object | Question it answers |
|--------|--------|---------------------|
| **Dual-View / p-adic Newton** | The map $N(x) = (2x^3+1)/(3x^2)$ over $\mathbb{Z}/2^k$ and $\mathbb{F}_p$ | *Where does arithmetic converge, and what is the geometry of its fixed points?* |
| **Butterfly Compiler** | The seed matrix $S$ and its Kronecker powers $S^{\otimes n}$ | *Does a linear/dynamical system admit a fast hierarchical transform?* |
| **Quantum Dual-View** | The spectral triple $(A, H, D)$ on the exponent space | *What does the fast transform look like as a quantum circuit?* |

**Thesis:** The dual-view's exponent space $\mathbb{Z}/2^{k-2}\mathbb{Z}$ is the *canonical domain* on which the butterfly compiler operates. The Newton projector is a *non-uniform* butterfly (position-dependent seeds) that collapses to a uniform QFT butterfly in the clean-prime vacuum sector.

---

## 2. Mathematical Dictionary

### 2.1 Seed ↔ Operator Correspondence

| Butterfly Compiler | Dual-View Arithmetic | p-adic Newton Dynamics |
|--------------------|----------------------|----------------------|
| Seed matrix $S$ | Shift operator $S: f(e) \mapsto f(e+1)$ | Newton map $N$ acting on functional graph |
| $S^{\otimes n}$ | Kronecker power on $\ell^2(C_{2^{k-2}})$ | $n$-th iterate $N^n$ on residue tree |
| Solvability series of $\langle S, S^T \rangle$ | Derived series of Lie algebra generated by $D = I-S$ and $D^\dagger$ | Galois solvability of dynatomic field tower |
| Nilpotent seed | Strictly upper-triangular $D$ (valuation collapse) | Clean prime functional graph (tree to 3 roots) |
| Unitary/conservative seed | $D$ on Hilbert space with $\|D\|=2$ | Isometric p-adic dynamics ($v_p(N(x_1)-N(x_2)) = v_p(x_1-x_2)$) |
| Expansive seed | Spectral radius $\rho > 1$ | Ghost cycles with multiplier $|\mu| > 1$ in Archimedean metric |
| Contractive seed | Attracting fixed point basin | Basin of attraction to cube roots |

### 2.2 Thermodynamic Classification ↔ Prime Classification

The `SpectralThermodynamics.analyze()` routine (from `dyadic_ml.bridge`) classifies a seed by its spectrum. Applied to the **Newton adjacency matrix** $B_p$ of the functional graph over $\mathbb{F}_p^*$ (ordered by basin depth):

| Prime | $B_p$ Spectrum | Thermodynamic Class | Butterfly Verdict |
|-------|---------------|---------------------|-----------------|
| **7, 103, 181** | $\{0, 0, \dots, 0, 1, 1, 1\}$ | **Nilpotent + 3 fixed points** | Compiles to 3-output multiplexer |
| Ghost-only (e.g., 13) | Roots of unity on unit circle | **Conservative/Unitary component** | Does NOT compile as collapse; requires iterative orbit tracing |
| Pole-only (e.g., 31) | $\{0\}$ on nilpotent blocks, no cycles | **Nilpotent** (but with 0 as absorbing state) | Partial compile — pole branches are dead code |
| Mixed (e.g., 43, 61) | Both unitary and nilpotent spectra | **Mixed phase** | Does NOT compile cleanly |

**Key observation:** Clean primes are the *only* primes where the Newton functional graph is a rooted forest (actually 3 trees). Everywhere else, the presence of cycles introduces rotational (unitary) components that prevent nilpotent collapse.

---

## 3. The Exponent Space as a Butterfly Domain

### 3.1 The Uniform Seed

In the dual-view framework, the exponent space is $C_N = \mathbb{Z}/N\mathbb{Z}$ with $N = 2^{k-2}$. The **shift operator** $S$ is the cyclic permutation:

$$S = \begin{pmatrix} 0 & 0 & \cdots & 0 & 1 \\ 1 & 0 & \cdots & 0 & 0 \\ 0 & 1 & \cdots & 0 & 0 \\ \vdots & \vdots & \ddots & \vdots & \vdots \\ 0 & 0 & \cdots & 1 & 0 \end{pmatrix}$$

The butterfly compiler's `fast_kron_power_transform` with seed $S$ over the real semiring is exactly the **cyclic convolution** on the exponent space. The QFT butterfly `qft_butterfly` replaces uniform seeds with position-dependent twiddle factors, but the *underlying graph structure* is the same butterfly topology.

### 3.2 The Dirac Operator as a Butterfly Seed

Your Dirac operator $D = I - S$ is a **difference operator** on the cyclic graph. In the butterfly compiler's language:

- **Seed**: $D$ (a $N \times N$ sparse matrix, but conceptually $2 \times 2$ at the finest level)
- **Kronecker power**: Not directly $D^{\otimes n}$, but the *hierarchical decomposition* of $D$ into block-circulant form
- **Solvability**: The Lie algebra $\langle D, D^\dagger \rangle$ is metabelian (derived algebra is central), so the butterfly compiler returns `is_solvable = True`

**Theorem to prove:** The operator $D = I - S$ on $C_N$ admits a butterfly decomposition into $O(\log N)$ stages of $2 \times 2$ block operations. The associated QFT on the exponent register is the quantum circuit implementation of this decomposition.

### 3.3 The Newton Projector as a Non-Uniform Butterfly

The Newton iteration in exponent space:

$$e \leftarrow e - \frac{g^e - a}{4 g^e L} \pmod{2^{k-2}}$$

is **not** a linear operation. It cannot be represented as a uniform-seed butterfly. However, in the QFT basis, multiplication by $g^e$ becomes a **phase shift**, and the Newton step becomes a **diagonal operator** followed by an inverse QFT:

$$\text{Newton}_a = \text{QFT}^{-1} \circ \text{Diag}(\phi_a(j)) \circ \text{QFT}$$

where the phase $\phi_a(j)$ depends on the target $a$. This is precisely the `PositionDependentOperad` from the butterfly compiler — different seeds at different butterfly positions.

**Theorem to prove:** The 2-adic Newton projector on the exponent space is a position-dependent butterfly operad. At clean primes, the position-dependence collapses to a uniform seed after $O(\log k)$ iterations, yielding a polynomial-depth quantum circuit.

---

## 4. Clean Primes as Vacuum Sectors in the Spectral Action

### 4.1 The Dynamical Dirac Operator at Prime $p$

Define the **Newton adjacency operator** on $\ell^2(\mathbb{F}_p^*)$:

$$(\mathcal{N}_p f)(x) = \begin{cases} f(N(x)) & \text{if } N(x) \neq 0 \\ 0 & \text{if } N(x) = 0 \text{ (pole)} \end{cases}$$

Then the dynamical Dirac operator is $\mathcal{D}_p = I - \mathcal{N}_p$.

### 4.2 Spectral Action

The spectral action at cutoff $\Lambda$:

$$\mathcal{S}_\Lambda(p) = \text{Tr}(f(\mathcal{D}_p / \Lambda))$$

For a clean prime, $\mathcal{N}_p$ is nilpotent in the basin-depth ordering, so $\mathcal{D}_p$ has:
- Eigenvalue $0$ with multiplicity 3 (the cube roots, fixed points)
- Eigenvalue $1$ with multiplicity $p-4$ (strictly upper-triangular part)

Choosing $\Lambda < 1$, the spectral action counts only the **vacuum degeneracy**:

$$\mathcal{S}_\Lambda(p) = 3 f(0) + (p-4) f(1/\Lambda)$$

As $p \to \infty$ among clean primes (if infinitely many exist), the ratio $\mathcal{S}_\Lambda(p)/p \to f(1/\Lambda)$. But the **vacuum term** $3f(0)$ is the topological invariant that distinguishes clean primes from all others.

### 4.3 Berry Phase from Curvature

Your dual-view defines curvature $F_{12} = [A_1, A_2]$ for gauge fields $A_1, A_2$ on the exponent space. In the quantum setting:

- **Gauge field** $A$: a perturbation of the Newton map parameter (e.g., varying the target $a$ adiabatically)
- **Adiabatic cycle**: Varying $a$ around a loop in $(\mathbb{Z}/2^k)^\times$
- **Berry phase**: $\gamma_B = \oint \langle \psi | \partial_\lambda | \psi \rangle d\lambda = \text{Tr}(F_{12} \cdot \text{Area})$

At clean primes, the vacuum subspace is 3-dimensional (spanned by cube roots). Adiabatically cycling $a$ around the unit group induces a **$U(3)$ Berry phase** on this subspace. The curvature $F_{12}$ is the obstruction to simultaneously diagonalizing the gauge fields.

**Open problem:** Compute the Berry phase explicitly for the loop $a \mapsto a \cdot g$ (multiplication by generator $g=5$) in the dual-view coordinates. Does it depend on the clean prime $p$?

---

## 5. The Compiler Architecture You Need

You have four layers in the dyadic implementation:

1. `dyadic_core` — arithmetic primitives
2. `dyadic_math` — cyclic group geometry, spectral triple, gauge fields, Newton projector
3. `dyadic_ml` — spectral classification, bridge analysis, butterfly seeds

The butterfly compiler adds:

1. `SeedThermodynamics` — spectral classification
2. `solvability_series` — Lie algebra derived series
3. `fast_kron_power_transform` — semiring butterfly engine
4. `qft_butterfly` — quantum circuit generator

**The missing synthesis layer** should be:

### 5.1 `quantum_dual_compiler` — Proposed Module

```
quantum_dual_compiler/
├── vacuum_analyzer.py      # Classify prime p by thermodynamics of N_p
├── butterfly_emitter.py    # Emit QFT + Newton projector circuit for clean p
├── berry_phase.py          # Compute F_12 and adiabatic phase for loops in unit group
├── valuation_guard.py      # Quantum error correction: detect v_1 + v_2 >= k
└── spectral_action.py        # Compute Tr(f(D/Lambda)) over clean-prime ensemble
```

### 5.2 Concrete Algorithm: Clean-Prime Circuit Generation

**Input:** Clean prime $p$, target unit $a \equiv 1 \pmod{4}$, precision $k$

**Output:** Quantum circuit $C_{p,a,k}$ that prepares the 2-adic coordinate state $|v\rangle \otimes |\alpha\rangle \otimes |e\rangle$

1. **State preparation**: Encode $a$ into the exponent register using Hensel bootstrap (classical pre-computation)
2. **QFT layer**: Apply `qft_butterfly` on the $e$-register (maps to frequency basis)
3. **Newton diagonal layer**: Apply position-dependent phase shifts encoding the 2-adic logarithm $\ln_2(5)$
4. **Inverse QFT**: Collapse to the exponent eigenstate
5. **Valuation measurement**: Measure $v$; if $v \geq k$, flag as vacuum collapse (error correction trigger)
6. **Sign extraction**: Measure $\alpha$ (single-qubit phase)

**Depth:** $O(\log k)$ from the QFT + $O(\log k)$ from Newton iterations = $O(\log k)$ total.

---

## 6. Open Theorems to Prove

### 6.1 Immediate (within existing framework)

1. **Solvability of the Dual-View Dirac Operator**
   - *Statement:* The Lie algebra $\mathfrak{g} = \langle I-S, I-S^{-1} \rangle$ on $\ell^2(C_N)$ is solvable of depth 2.
   - *Status:* Essentially proven by abelianness of the cyclic group. Needs formal write-up.

2. **Butterfly Compilation of the QFT on Exponent Space**
   - *Statement:* The QFT on $C_{2^{k-2}}$ is generated by the butterfly operad with seeds $S_{km} = \frac{1}{\sqrt{2}} \begin{pmatrix} 1 & \omega^m \\ 1 & -\omega^m \end{pmatrix}$.
   - *Status:* Standard Cooley-Tukey, but needs explicit mapping to the dual-view's $(v, \alpha, e)$ registers.

3. **Nilpotency of Clean-Prime Newton Adjacency**
   - *Statement:* For $p \in \{7, 103, 181\}$, the operator $\mathcal{N}_p$ is nilpotent in the basin-depth basis.
   - *Status:* Verified computationally to $p < 10^4$. Needs algebraic proof using the dynatomic non-cube condition.

### 6.2 Medium-term (requires new constructions)

4. **Clean Prime ↔ Butterfly-Compilable Vacuum**
   - *Statement:* A prime $p$ is clean iff the Newton adjacency $\mathcal{N}_p$ is butterfly-compilable as a nilpotent 3-output multiplexer.
   - *Significance:* This makes "clean prime" a property of the *algorithmic complexity* of the functional graph, not just dynamics.

5. **Quantum Speedup of 2-adic Discrete Log**
   - *Statement:* The 2-adic discrete logarithm via Newton iteration on the exponent register can be implemented as a quantum circuit of depth $O(\log k)$ using the QFT butterfly, achieving a **quadratic speedup** over classical Hensel lifting.
   - *Caveat:* The speedup is in *circuit depth*, not query complexity. The initial state preparation still requires classical pre-computation of $L = \ln_2(5)/4$.

6. **Berry Phase Formula for Generator Loop**
   - *Statement:* For the adiabatic loop $a(t) = 5^t$ in $(\mathbb{Z}/2^k)^\times$, the Berry phase on the vacuum subspace at clean prime $p$ is $\gamma_B = \frac{2\pi}{3} \cdot \frac{p-1}{3} \pmod{2\pi}$.
   - *Speculative:* Based on the fact that 5 generates the cyclic part and cube roots are the fixed points.

### 6.3 Long-term (speculative)

7. **Density Zero as Spectral Gap Closing**
   - *Statement:* The density-zero theorem for clean primes is equivalent to the statement that the spectral gap of $\mathcal{D}_p$ closes as $p \to \infty$ through non-clean primes.
   - *Interpretation:* Clean primes are rare because they require a spectral gap (nilpotency) that generic primes don't have.

8. **Unified Clifford Constant in Quantum Hardware**
   - *Statement:* The hardware cliff detector $c(g) = \max(0, \text{tzcnt}(g+123)-2)$ can be implemented as a quantum oracle using the valuation register $|v\rangle$ as a control qubit, triggering error correction when $v$ exceeds the budget.
   - *Connection:* Your existing C++/Python cliff detector becomes a quantum predicate.

---

## 7. Research Trajectory: Recommended Order of Attack

Given your 5-6 threads and limited mobile access, here's the priority order:

### Phase 1: Formalize the Bridge (1-2 weeks)
- Write the **Solvability Theorem** for $D = I-S$ on the exponent space
- Map the `solvability_series` output explicitly for $S$ = cyclic shift, $S$ = Newton adjacency at clean primes
- Document the thermodynamic classification table (Section 2.2 above) with proofs

### Phase 2: Build the Compiler Pass (2-3 weeks)
- Implement `vacuum_analyzer.py`: take a prime $p$, compute functional graph, classify by thermodynamics
- Implement `butterfly_emitter.py`: for clean primes, emit the QFT+Newton circuit
- Run end-to-end: input $p=7$, target $a$, output circuit diagram

### Phase 3: Quantum Formalization (ongoing)
- Prove the **position-dependent butterfly operad** theorem for the Newton projector
- Compute the **Berry phase** for the generator loop (even numerically for $p=7, 103, 181$)
- Connect to spectral action: plot $\mathcal{S}_\Lambda(p)$ for clean vs. non-clean primes

### Phase 4: Hardware Integration (future)
- Map the cliff detector to quantum error correction predicates
- Design the valuation-collapse guard as a quantum control operation

---

## 8. Summary: The Single Sentence

> The dual-view's exponent space is the canonical domain of the butterfly compiler; clean primes are the nilpotent vacuum sectors where the Newton projector collapses to a polynomial-depth quantum circuit; and the spectral geometry of the 2-adic ring is the Hamiltonian that governs this evolution.

---

*Document generated from synthesis of uploaded research materials and conversation history.*
*Date: 2026-05-14*
